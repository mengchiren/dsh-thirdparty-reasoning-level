# 第三方模型推理等级配置 · 使用说明

> 场景：DSH 里自定义厂商（火山方舟 Coding Plan）无法设置模型推理等级。
> 结论：DSH **原生支持**第三方模型推理等级，缺的只是厂商配置里的能力声明。
> 本目录交付：已修正的 `settings.yaml`（修改前备份留在本机
> `settings.yaml.backup-*`，不入库）、可复用模板 `fangzhou-coding-plan-provider.yaml`、本说明。

---

## 1. 根因（为什么之前没有推理等级）

DSH 的第三方厂商插件 `llm-pi-ai` 按"模型"声明推理能力：

- 每个模型声明 `reasoningEfforts`（等级 → 线上参数值）。**不声明 = 该模型不支持推理等级**，模型选择器就不显示等级控件；
- 推理方言 `compat.thinkingFormat`（deepseek / qwen / openai…）**只对 `openai-completions` 协议生效**；
- `anthropic-messages` 协议只有"开/关 + token 预算"，天然表达不了等级。

原配置两处都踩了：模型没声明 `reasoningEfforts`，且协议用的是 `anthropic-messages`（后来又试了 `openai-responses`，也是错的报文格式——方舟 `/v3` coding 端点是 **Chat Completions** 报文，对应 `openai-completions`）。

## 2. 现在的配置做了什么

| 项 | 值 | 说明 |
|---|---|---|
| `api` | `openai-completions` | 方舟 coding 端点的真实报文格式（第三方实测验证） |
| `baseURL` | `https://ark.cn-beijing.volces.com/api/coding/v3` | OpenAI 兼容面 |
| 厂商级 `reasoning: high` | 默认等级 | 所有模型默认 High，并防止旧会话残留越界等级报错 |
| deepseek-v4-flash/pro | `low / high / max` 三档 | `thinking:{type:"enabled"}` + `reasoning_effort:<档位>`；官方 API 档位表背书（medium/xhigh 被服务端收敛为 high） |
| glm-5.3 | `off(low)/low/medium/high/max` | 直接发 `reasoning_effort`；思考关不掉，low≈off |
| `maxTokens: 128000` | 修复原 256000 | 方舟拒绝 >128000（400 InvalidParameter） |

生效方式：`dsh-settings-file` 监听 `settings.yaml`，**保存即热生效，无需重启**。

## 3. 如何验证

1. 打开右上角**模型选择器**，选 `方舟` 组：
   - `deepseek-v4-flash` / `deepseek-v4-pro` → 触发器显示 `模型名 · High`，下拉一项 High；
   - `glm-5.3` → 下拉有 **Off / Low / Medium / High / Max**，默认 High。
2. 选一个等级后发一条消息，模型卡片会显示 `当前模型：… · 推理等级：…`。
3. 如果下拉没出现：先**刷新页面**（模型目录在打开选择器时加载）；仍无则检查
   `settings.yaml` 是否被设置页写回覆盖（见 §5 坑 4）。

## 4. 各模型等级语义速查（实测/第三方验证）

| 模型 | UI 档位 | 线上参数 | 备注 |
|---|---|---|---|
| deepseek-v4-flash / pro | Low / High / Max | `thinking: {type:"enabled"}` + `reasoning_effort: low\|high\|max` | 官方 API 档位表：low→low、medium→high、high→high、xhigh→high、max→max；方舟端点只验证过 high，low/max 若报 400 就删对应行；UI 不显示 Off（思考默认开启，关闭需发 `thinking:{type:"disabled"}`，未验证） |
| glm-5.3 | Off/Low/Medium/High/Max | `reasoning_effort: low\|medium\|high\|max` | 思考不可关闭，Off 实际发 `low` |
| kimi-k2.7-code（扩展） | High | `enable_thinking: true` | `thinkingFormat: qwen` |
| glm-5.2 / minimax-*（扩展） | 无 | — | 无思考开关，`reasoningEfforts: false` |

## 5. 常见坑

1. **maxTokens 超限**：方舟 coding 端点 `max_tokens > 128000` 直接 400。所有模型统一 `128000`。
2. **`agent-default-model` 里的推理等级**：切到方舟模型时填模型支持的等级
   （deepseek 用 `high`；glm-5.3 用 `low/medium/high/max`）。由于厂商级
   `reasoning: high` 已让适配器声明默认值，残留的 `max` 会被丢弃走默认 high，
   不会报错；但为了意图明确，建议改掉。
3. **glm-5.3 的 Off 不等于真关**：平台不支持关闭思考，`off` 映射为 `reasoning_effort: "low"`（≈ 近乎不思考）。想省时省钱用 Low/Off 即可。
4. **设置页写回**：已实测（2026-08-20）设置页保存后 `reasoningEfforts`/`compat`
   会原样保留（dsh-settings-file 用叶子级 diff 补丁，未知字段不丢）。不过 GUI
   大改后仍建议扫一眼 YAML 确认。
5. **编码（乱码）**：settings.yaml 是 UTF-8；用默认 GBK 的工具查看会显示
   `鏂硅垷`（实为"方舟"的 UTF-8 字节被 GBK 误读）——**只看不写没事，一旦用
   GBK 工具保存就会真的损坏文件**。请用 UTF-8 编辑器（VS Code / 现代记事本），
   或把 `displayName` 改为纯 ASCII（如 `Fangzhou`）彻底规避。
6. **想给 deepseek 增加可选 Off**：把 `off: null` 改成 `off: disabled`
   （会发送 `thinking: {type:"disabled"}`）；若方舟拒绝（400）再改回 `null`。
7. **deepseek 的 low/max 档位**：官方 API 明确支持（见 §4 档位表），配置里已默认
   声明。方舟 coding 端点只验证过 high，若选中 low/max 后请求报 400
   `InvalidParameter`，删掉 `reasoningEfforts` 里对应两行即可回退为仅 High。
   `medium`/`xhigh` 不必声明——服务端会收敛为 high，声明了也只是重复档位。

## 6. 迁移到新机器 / 新厂商

- 新机器：把 `fangzhou-coding-plan-provider.yaml` 的 `fangzhou:` 段粘进新机的
  `%USERPROFILE%\.dsh\settings.yaml`，并确保环境变量 `FANGZHOU_API_KEY` 存在。
- 新厂商（OpenAI 兼容）：复制厂商段，改 `baseURL`/`apiKeyEnv`/`displayName`，
  按该厂商各模型的真实能力声明 `reasoningEfforts` + `compat.thinkingFormat`
  （方言枚举：`openai`/`deepseek`/`openrouter`/`together`/`zai`/`qwen`/
  `string-thinking`/`ant-ling`）。不确定能力的模型宁可 `reasoningEfforts: false`，
  也不要谎报——否则选中不支持的等级会直接报 `UNSUPPORTED_REASONING_EFFORT`。

## 8. pi-ai 方舟兼容补丁（node_modules，2026-08-20 已打，实测校准）

**背景**：pi-ai（DSH 底层 LLM 库）对未知网关默认假设"支持 developer 角色"，
方舟 coding 端点不认（实测 400 `messages.role ... invalid value: developer`）。

**已做**：给 pi-ai 的 openai-completions 探测逻辑打了 3 处补丁，仅针对
`volces.com`：`supportsDeveloperRole=false`（system 角色）、
`maxTokensField=max_tokens`（实测方舟两个字段都认，取扩展验证过的写法）、
`requiresReasoningContentOnAssistantMessages=true`（deepseek 官方要求工具轮回传思维链）。

**实测校准记录（重要，别再改回去）**：
- **不要动 `supportsStrictMode`**：方舟和 wawa 中转站都【要求工具必须带
  `strict` 字段】，省略会 400 `could not parse the JSON body`；`strict: false`
  双方都接受。即 strict 模式保持 pi-ai 默认开启即可；
- `reasoning_effort: "max"` 在方舟 deepseek-v4-flash 上**实测可用**（2026-08-20
  直连端点验证），max 档位声明成立；
- 中转站（wawa）**不需要本补丁**：实测接受 developer 角色 / strict:false /
  max_completion_tokens / reasoning_effort。

**注意**：
- 补丁在 `node_modules` 里，HMR 不会热载——**需要重启 DSH 才生效**；
- **DSH 升级 / npm 重装会重置 node_modules，补丁会丢失**；
  重装后运行本目录 `reapply-pi-ai-ark-patch.ps1` 一键重打（幂等），再重启；
- 原始文件备份留在本机工作区 `openai-completions.js.original`（不入库）；
- 若方舟后续原生支持 developer 角色或 pi-ai 新版内置识别，可移除补丁。

## 9. 中转站（wawa 示例）的推理等级配方

中转站与官方端点不同：模型能力 = 中转站透传 + 后端真实模型，**只能声明后实测**。

- **baseURL 必须带路径前缀（如 `/v1`）**：pi-ai 拼接 `{baseURL}/chat/completions`，
  不带前缀会打到网站首页——把 HTML 当流解析，报
  `Stream ended without finish_reason` 并无限重试（wawa 实测踩坑，2026-08-20）。
  中转站控制台一般会写明 base_url，照抄即可。
- **OpenAI 系模型（gpt-5.x 等）**：档位 `off/low/medium/high`，
  **没有 max**（OpenAI 合法值仅 minimal/low/medium/high，声明 max 会 400）；
  思考无法真正关闭，`off` 应映射到最低档 `minimal`（否则"Off"实际是默认档）；
  线上参数为 `reasoning_effort: <档位>`（`compat: {supportsReasoningEffort: true}`，
  无需 thinkingFormat）。
- **deepseek 系**：同方舟配方（`off/high/max` + `thinkingFormat: deepseek`）。
- **实测流程**：声明 → 刷新页面 → 各档位各发一条消息 → 报 400
  `InvalidParameter` 就删掉对应档位；报 `Stream ended without finish_reason`
  先查 baseURL 路径，再查是否要求 strict 字段。

## 10. 为什么不做成动态插件

动态插件（Cordis Plugin）是**进程级临时**的：重启即失效、每次会话都要重新运行，
且运行时改配置/拦请求都属于重复造原生轮子。推理等级这条链路（声明 → 校验 →
UI 选择器 → 请求映射）DSH 已端到端原生实现，配置声明是唯一且持久的事实源。
